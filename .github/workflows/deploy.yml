name: Deploy Application
on:
  push:
    branches: [main, feature/render-deployment-workflow]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy on Render
        run: curl -fsSL "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
